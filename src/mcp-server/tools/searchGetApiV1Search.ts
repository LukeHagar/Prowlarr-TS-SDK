/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { searchGetApiV1Search } from "../../funcs/searchGetApiV1Search.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApiV1SearchRequest$inboundSchema,
};

export const tool$searchGetApiV1Search: ToolDefinition<typeof args> = {
  name: "search-get-api-v1-search",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await searchGetApiV1Search(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
