/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tagGetApiV1TagId } from "../../funcs/tagGetApiV1TagId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApiV1TagIdRequest$inboundSchema,
};

export const tool$tagGetApiV1TagId: ToolDefinition<typeof args> = {
  name: "tag-get-api-v1-tag-id",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tagGetApiV1TagId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
