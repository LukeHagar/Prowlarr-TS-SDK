/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { authenticationGetLogout } from "../../funcs/authenticationGetLogout.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$authenticationGetLogout: ToolDefinition = {
  name: "authentication-get-logout",
  description: ``,
  tool: async (client, ctx) => {
    const [result, apiCall] = await authenticationGetLogout(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
