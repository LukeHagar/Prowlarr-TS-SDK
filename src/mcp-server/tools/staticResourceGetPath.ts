/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { staticResourceGetPath } from "../../funcs/staticResourceGetPath.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetPathRequest$inboundSchema,
};

export const tool$staticResourceGetPath: ToolDefinition<typeof args> = {
  name: "static-resource-get-path",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await staticResourceGetPath(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
