/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tagDetailsGetApiV1TagDetail } from "../../funcs/tagDetailsGetApiV1TagDetail.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$tagDetailsGetApiV1TagDetail: ToolDefinition = {
  name: "tag-details-get-api-v1-tag-detail",
  description: ``,
  tool: async (client, ctx) => {
    const [result, apiCall] = await tagDetailsGetApiV1TagDetail(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
