/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customFilterPutApiV1CustomfilterId } from "../../funcs/customFilterPutApiV1CustomfilterId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutApiV1CustomfilterIdRequest$inboundSchema,
};

export const tool$customFilterPutApiV1CustomfilterId: ToolDefinition<
  typeof args
> = {
  name: "custom-filter-put-api-v1-customfilter-id",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customFilterPutApiV1CustomfilterId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
