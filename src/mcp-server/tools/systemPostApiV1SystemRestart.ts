/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { systemPostApiV1SystemRestart } from "../../funcs/systemPostApiV1SystemRestart.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$systemPostApiV1SystemRestart: ToolDefinition = {
  name: "system-post-api-v1-system-restart",
  description: ``,
  tool: async (client, ctx) => {
    const [result, apiCall] = await systemPostApiV1SystemRestart(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
