/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { applicationPutApiV1ApplicationsId } from "../../funcs/applicationPutApiV1ApplicationsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutApiV1ApplicationsIdRequest$inboundSchema,
};

export const tool$applicationPutApiV1ApplicationsId: ToolDefinition<
  typeof args
> = {
  name: "application-put-api-v1-applications-id",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await applicationPutApiV1ApplicationsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
