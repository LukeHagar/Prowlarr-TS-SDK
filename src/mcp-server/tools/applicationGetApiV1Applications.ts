/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { applicationGetApiV1Applications } from "../../funcs/applicationGetApiV1Applications.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$applicationGetApiV1Applications: ToolDefinition = {
  name: "application-get-api-v1-applications",
  description: ``,
  tool: async (client, ctx) => {
    const [result, apiCall] = await applicationGetApiV1Applications(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
