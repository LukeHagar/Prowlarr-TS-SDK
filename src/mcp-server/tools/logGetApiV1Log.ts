/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { logGetApiV1Log } from "../../funcs/logGetApiV1Log.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApiV1LogRequest$inboundSchema,
};

export const tool$logGetApiV1Log: ToolDefinition<typeof args> = {
  name: "log-get-api-v1-log",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await logGetApiV1Log(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
