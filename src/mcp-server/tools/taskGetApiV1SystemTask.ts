/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { taskGetApiV1SystemTask } from "../../funcs/taskGetApiV1SystemTask.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$taskGetApiV1SystemTask: ToolDefinition = {
  name: "task-get-api-v1-system-task",
  description: ``,
  tool: async (client, ctx) => {
    const [result, apiCall] = await taskGetApiV1SystemTask(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
