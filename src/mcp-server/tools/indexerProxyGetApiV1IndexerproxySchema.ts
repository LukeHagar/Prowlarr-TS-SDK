/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { indexerProxyGetApiV1IndexerproxySchema } from "../../funcs/indexerProxyGetApiV1IndexerproxySchema.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$indexerProxyGetApiV1IndexerproxySchema: ToolDefinition = {
  name: "indexer-proxy-get-api-v1-indexerproxy-schema",
  description: ``,
  tool: async (client, ctx) => {
    const [result, apiCall] = await indexerProxyGetApiV1IndexerproxySchema(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
