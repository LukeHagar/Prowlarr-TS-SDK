/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { newznabGetIdDownload } from "../../funcs/newznabGetIdDownload.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetIdDownloadRequest$inboundSchema,
};

export const tool$newznabGetIdDownload: ToolDefinition<typeof args> = {
  name: "newznab-get-id-download",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await newznabGetIdDownload(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
